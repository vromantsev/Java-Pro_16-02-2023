package ua.hillel.web;

public class WebProjectsDemo {

    // web-project - data exchange, user allowed to interact with the web-app, mandatory authentication
    // vs
    // web-site - static information, showed on the web-pages, user interactions are now allowed, authentication is not mandatory

    // web-app  -> UI (client) <--> backend (server) -> Client-Server architecture
}
